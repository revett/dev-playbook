---
- name: Create `~/.ssh` directory
  file:
    path: ~/.ssh
    state: directory

- name: Create empty `~/.ssh/id_rsa` file
  file:
    path: ~/.ssh/id_rsa
    state: touch

- name: Create empty `~/.ssh/id_rsa.pub` file
  file:
    path: ~/.ssh/id_rsa.pub
    state: touch
